name: Convert Prompts to JSON

on:
  push:
    paths:
      - "prompts/*.prompt.yaml"
      - "prompts/*.prompt.yml"

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install yq
        run: sudo apt-get install -y yq

      - name: Convert YAML prompts to JSON
        run: |
          for file in prompts/*.prompt.yaml prompts/*.prompt.yml; do
            if [ -f "$file" ]; then
              json_file="${file%.*}.json"
              echo "Converting $file → $json_file"
              yq '.your_filter' input.yaml > output.json
            fi
          done

      - name: Commit converted JSON files
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add prompts/*.prompt.json
          git commit -m "Auto-convert YAML prompts to JSON"
          git push
